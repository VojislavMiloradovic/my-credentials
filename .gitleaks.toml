title = "Gitleaks Configuration"

[allowlist]
description = "Global allowlist for non-sensitive aggregated text and GUID patterns"
paths = [
    '''(^|/)llms(-full)?\.txt$''',
    '''(^|/)archive/.*'''
]
regexes = [
    '''"(?:activityId|id|studyGuideUids)":\s*"[a-fA-F0-9-]{36}"'''
]
